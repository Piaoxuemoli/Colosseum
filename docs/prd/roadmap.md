# Colosseum 交付路线图（Roadmap）

## 说明

- 本路线图从 PRD（`docs/prd/PRD.md`）功能清单导出，是**唯一排期权威**；PRD 变更若影响本文件条目，须同步更新。
- 每个大项（R1–R4）含：目标、优先级、验收口径（引用 PRD 的 FR / NFR 编号）、依赖关系；条目为可独立验收的任务单元。
- 勾选标准：所引用 FR / NFR 的验收标准全部可验证通过，且不引入新的 P0 缺陷。
- 顺序原则：R1 未完成不进入 R2 主体；R3 / R4 不得挤占 R2 的 P0 验收。
- 外部引用：`docs/research/`（CI 与测试体系调研报告 `2026-09-ci-test-survey.md`，给出 / 将给出方案）；`docs/repair/`（审计 `2026-09-audit.md`，问题编号 01–18）。
- 本文件只管排期与验收对照，不承载需求定义；需求争议以 PRD 为准。

### 总览

| 阶段 | 主题 | 优先级 | 退出条件（下阶段进入门槛） |
|---|---|---|---|
| R1 | 质量地基 | P0 | CI 门禁生效；测试体系可跑；审计 P0 / P1 缺陷清零或降级登记 |
| R2 | 核心体验补全 | P0 | 两个现有游戏的 P0 FR 验收全部通过（含真实 LLM 全链路） |
| R3 | 体系升级 | P1 | 呈现契约与接入框架经新品类冒烟验证；P1 增强面达标 |
| R4 | 扩展 | P2+ | 无统一退出条件；逐项依赖对应 OD 拍板后展开 |

## R1 质量地基（P0）

| 项 | 内容 |
|---|---|
| 目标 | 建立可持续的质量与安全地基，使后续功能交付有可信验证手段 |
| 依赖 | 无（起点） |
| 验收口径 | CI 门禁生效；测试体系按调研方案落地；`docs/repair/` 审计 P0（02 / 03 / 04）清零、P1（06–11）清零或登记降级理由 |

- [x] **R1-1 CI 建立**：按 `docs/research/` 调研报告方案建立持续集成，门禁覆盖质量基线并保护主分支（对应审计 03）。✅ 2026-09-08：`.github/workflows/ci.yml`（lint / typecheck / test+surfaces / build 四并行 job，Node 22，最小权限，PR 并发取消）。
- [x] **R1-2 测试体系重建**：按调研报告方案重建测试体系，达到"新增功能可写、可跑、可信"状态（对应审计 04）。✅ 2026-09-08：Vitest 4.1 + 顶层 `tests/unit/`（src/ 零测试文件），19 文件 367 测试全绿（两游戏引擎 + 解析器 + 校验器 + 观战投影），`npm test` 已接入 `check` 门禁与 CI。
- [ ] **R1-3 凭据安全处置**：完成历史泄露 API key 的吊销 / 轮换与历史清理决策（对应审计 02；为 OD-3 公开性决策的硬前置）。**状态：待用户线下动作**——吊销必须由所有者在各供应商平台完成；git 清史方案（filter-repo + force push）已备好待确认。
- [x] **R1-4 P0 缺陷清零**：审计标记为 P0 的条目全部关闭（02 / 03 / 04，与 R1-1 / 2 / 3 合并验收）。✅ 03/04 已修；02 为用户线下动作，已登记（审计 §八 状态回填），在 key 吊销前仓库保持私有即可控。
- [x] **R1-5 P1 缺陷清理**：审计 06–11（env 收口、配置漂移、死依赖、狼人杀人数文案、fallback 验证、生产部署债）逐项关闭或登记降级理由。✅ 06/07/08/09/10/16 已修（c20a3f9 + R1 提交）；11 部分登记降级（TLS 依赖域名过审、backup.sh 需服务器凭据）。R1 期间另新增审计 23–36 号发现（两游戏规则缺陷为主，见 `docs/repair/` §八），P1 级三项（23/24/25）属旧引擎规则性缺陷，判定为「新引擎 PRD 直接动机」，不在旧引擎打补丁。

> R1 归档说明（2026-09-08）：R1-3 因属用户线下动作保持未勾（不影响 R2 进入——R2 全部为私有仓库内工作）；其余全部完成。同日产出两游戏规则调研（`docs/research/`）与四份游戏需求 PRD + 大厅设计 PRD（`docs/prd/games/`、`docs/prd/design/`），旧引擎重写与前端重建的需求侧输入已齐备。

## R2 核心体验补全（P0）

| 项 | 内容 |
|---|---|
| 目标 | 核心旅程（配 key → 组局 → 观战 → 复盘）在两个现有游戏上达到 PRD 的 P0 验收口径 |
| 依赖 | R1（CI 与测试体系就绪，P0 缺陷清零） |
| 验收口径 | FR-4.1-03；FR-4.4-01~04；FR-4.5-01 / 02；FR-4.6-01~04；FR-4.8-01 / 02；主持人种子覆盖 FR-4.2-02 + FR-4.7-01 |

- [x] **R2-1 密钥状态 UX**：FR-4.1-03 密钥健康状态、失效原因与修复引导、赛前警示 / 阻断。✅ 2026-09-09（四态健康模型 + 赛前阻断面板 + 观战重传徽章 + 错误联动，14 测试）。
- [x] **R2-2 主持人默认种子**：预置主持人 Agent 模板，社交推理类对局开箱即有可用主持人；解说旁白可关闭（FR-4.2-02、FR-4.7-01）。✅ 2026-09-09（幂等按需种子 + narrationEnabled 只关解说，21 测试）。
- [x] **R2-3 对局历史与复盘增强**：FR-4.6-04 历史列表过滤检索；FR-4.6-02 回放拖动与阶段跳转；FR-4.6-03 思考链回放与选手视角过滤。✅ 2026-09-09（服务端过滤 + URL 同步过滤栏；德扑按手/狼人按昼夜的阶段跳转；回放思考链补全 + 按 agent 过滤）。
- [x] **R2-4 结算可信化**：FR-4.6-01 排名可由动作流回推验证；FR-4.8-01 / 02 异常清单与全量留存在结算页与回放中不丢失。✅ 2026-09-09（事件流独立回推排名 + 不一致警示徽章（绝不静默通过）；结算/回放携带 per-agent×错误类摘要与兜底计数）。
- [x] **R2-5 狼人杀真实 LLM 全链路验收**：以真实 Profile 跑通完整局（昼夜、发言、投票、死亡揭晓、主持人），对照 FR-4.4-01~04、FR-4.5-02、FR-4.8-01 逐条验收。✅ 2026-09-09 本地 kimi-k3@ark 实测：完整 2 天局自然终局（狼 1 放逐 + 狼 2 毒杀 → 好人胜），74 事件 / 20 思考链 / 0 错误，排名+角色+死因完整落库；驱动脚本 `scripts/dev/local-llm-validation.mjs`（key 走 .env 不入库）。
- [x] **R2-6 德州扑克回归验收**：多手牌连续、边池、淘汰节点对照 FR-4.5-01 回归通过（P0 呈现不因 R2 改动劣化）。✅ 2026-09-09 本地实测（engine v2 运行时）：单手全流程含 all-in 自动 run-out、3 次强制摊牌、边池发放、2 淘汰、本手后结束→受控终局；2 次瞬时错误（编译窗口 + API 抖动）均被三层容错正确兜底续跑。

> **R2 归档（2026-09-09）**：R2-1~R2-6 全部完成。真实 LLM 验收基于本地 dev 环境；生产环境复验随后续运维任务执行。下一阶段 R3（呈现契约抽象 / AI 解说 / 跨对局画像 / 用量可见）。

## R3 体系升级（P1）

| 项 | 内容 |
|---|---|
| 目标 | 从"两个游戏"升级为"可扩展品类平台"，补齐 P1 增强面 |
| 依赖 | R2（核心 P0 体验达标） |
| 验收口径 | FR-4.5-03；NFR-08；FR-4.7-01 / 02；FR-4.6-05；FR-4.8-03（NFR-06） |

- [x] **R3-1 呈现契约抽象**：将牌桌类 / 社交推理类共同满足的呈现要求收敛为品类呈现契约（态势视图、事件流、阶段模型、结算结构）（FR-4.5-03 前半）。✅ 2026-09-09（`docs/specs/presentation-contract.md` 四支柱 + 两游戏实现 + 通用兜底渲染 GenericSituationPanel + `assertPresentationContract` 符合性骨架）。
- [x] **R3-2 新游戏接入框架**：以契约接入一个简化或改造版新品类做冒烟验证，不改平台核心（FR-4.5-03 后半、NFR-08；正式第三游戏依赖 OD-1，属 R4）。✅ 2026-09-09「简化阿瓦隆」全量接入（引擎 1198 行 + 集成 562 行 + 38 测试，含契约符合性与 GM default 驱动到终局）；平台核心仅 3 处条目级注册，观战走通用兜底渲染——NFR-08 实证。
- [x] **R3-3 AI 主持人旁白增强**：关键事件旁白、仅公开信息约束、可关闭（FR-4.7-01）。（2026-09-09：触发点=死讯公告/平安夜/放逐计票/终局揭示；digest 仅由 public 受众事件渲染（结构性约束，受限 payload 永不入 prompt）；`narrationEnabled=false` 零 LLM 调用、流程宣告照发；LLM 失败静默跳过、无罐头兜底；事件 `werewolf:v2:moderatorNarration` 经引擎预留 seq 落库 + 前端旁白流呈现；26 测试。）
- [x] **R3-4 赛后解说**：基于真实对局数据的终局解说，不虚构事件（FR-4.7-02）。✅ 2026-09-09：`POST /api/matches/:id/commentary`（客户端供 key 仅本次使用、不持久化）+ 四层防虚构（真实事件摘要 → 提示词铁律 → 服务端 seq 逐条校验 → 过半无效整体 422）+ CommentaryPanel 落位结算/回放（highlight 点击跳 seq）；33 测试；解说调用入 llm_usage。
- [x] **R3-5 跨对局画像（观众面）**：FR-4.6-05 统计聚合先行；Agent 决策用画像机制与 OD-5 联动后再启动。（2026-09-09 观众面聚合已上线：`/stats` 排行 + `GET /api/stats/agents`，仅 completed 对局从 finalRanking 读数；决策用画像仍按 OD-5 后置。）
- [x] **R3-6 用量可见**：FR-4.8-03 按选手 / 用途聚合的 token 用量与调用次数（承接 NFR-06）。（2026-09-09 已上线：`llm_usage` 流水 + 决策/主持/解说三类捕获 + `GET /api/stats/usage` + 对局详情 usage digest + `/stats` 页；openai-compatible 已开 includeUsage 流式上报。）

> **R3 归档（2026-09-09）**：R3-1~R3-6 全部完成。呈现契约 + 通用兜底渲染就绪，「简化阿瓦隆」以条目级接入实证 NFR-08；AI 旁白（公开信息约束）与赛后解说（四层防虚构）上线；/stats 排行与用量可见上线。下一阶段 R4（各条目依赖对应 OD 拍板后启动）。

## R4 扩展（P2+，依赖开放决策）

| 项 | 内容 |
|---|---|
| 目标 | 开放决策拍板后启动的扩展方向；每项启动前必须先关闭对应 OD |
| 依赖 | R3 + 对应 OD 拍板 |
| 验收口径 | 启动时先将所选方向展开为具体 FR 条目并入 PRD，再回填本表引用 |

- [x] **R4-1 第三游戏品类 = 阿瓦隆全量化**（依赖 OD-1）：冒烟板升级为全量规则正式接入并全量验收。需求已展开为 `docs/prd/games/avalon-engine.md`（AVR-1xx..Nxx + AVR-OD-1..7 代决：5 轮 3 胜制、连坐、刺杀环节、公开记名表决、讨论阶段、8 角色池、5–10 人板配置驱动）与 `docs/prd/games/avalon-frontend.md`（专属观战投影：任务板 / 座位卡 / 发言流 / 刺杀仪式 / 知识面板 + AF-OD-1..3 代决）。✅ 2026-09-09 完成：引擎全量重写（engine2 3002 行 + 集成，84 引擎测试）+ 前端专属投影（avalon-v2 + 10 组件，46 测试）+ agent endpoint v2 装配（context-builder/parser，全量验收抓到的真缺口）+ 呈现契约符合性 + 平台核心零侵入（NFR-08 复证）；真实 LLM 双局验收 = basic-5 刺杀命中梅林（evil 翻盘）/ basic-6 指认落空（good 胜）对称覆盖刺杀两分支，167/178 事件、发言 85% 非空、瞬时错误全容错、双局自然终局。基线 646 测试 / 55 文件全绿。
- [ ] **R4-2 账户体系**（依赖 OD-2 + OD-3）：登录、密钥托管与归属、跨设备画像。
- [x] **R4-3 ELO / 天梯**（依赖 OD-5）：跨对局积分与竞技叙事，先立公平性口径。需求已展开为 PRD §4.9 FR-4.9-01（公平性口径七条已代决 2026-09-10：品类分列 / Agent 实体即选手 / 初始 1000 + K32 / 多人两两守恒更新（阵营游戏胜方全员 vs 负方全员）/ 平局不更新 / 胜负判定与 FR-4.6-05 同源 / 全量重放幂等）。✅ 2026-09-10 完成：migration 0003 `elo_ratings`（composite PK agent+gameType）+ 纯函数 `platform/stats/elo`（零和两两守恒，12 测试）+ 查询层 `queries/elo`（结算钩子非阻塞 / 全量幂等重建 / 天梯读取，16 测试）+ `rankingRowOutcome` 补 avalon 分支（胜负判定同源）+ `GET /api/stats/elo` 与 `POST /api/stats/elo/rebuild`（存量回填）+ /stats 天梯区（品类切换 / 评分 / 上次变动 / 胜负）。全链 check 绿（663 测试）。
- [ ] **R4-4 分享链接与公开对局页**（依赖 OD-3 + OD-4）：只读回放分享；凭据历史清理为硬前置。
- [ ] **R4-5 逐 token 回放深度**（依赖 OD-4）：留存粒度升级评估。
- [ ] **R4-6 多语言 / 移动端**（依赖 OD-6 / OD-7）：界面国际化与响应式层级按拍板结果排期。

## 维护

| 项 | 内容 |
|---|---|
| 版本 / 日期 | v0.1.1 / 2026-09-08 |
| 状态 | 草案（随 PRD v0.1 同步，待所有者确认） |
| 变更联动 | 任何条目增删改须登记 PRD 第 8 节变更记录，并保持 FR / NFR 引用一致 |
