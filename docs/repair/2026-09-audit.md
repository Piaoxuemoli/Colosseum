# Colosseum 仓库大修审计（2026-09）

- 审计日期：2026-09-07
- 审计基线：main 分支（a2ui 合并之后）
- 范围说明：全仓库静态审计 + git 历史核查，覆盖六个维度——安全（凭据泄露）、工程基建（CI / 测试）、配置漂移、架构债、文档债、部署债。本文是权威「大修文档」，下表编号（01–22）可作为后续 plan / issue / 提交信息的引用键。
- 路径约定：本文引用的文档路径一律使用重构后的新规范路径：`docs/prd/`（需求）、`docs/rules/`（规则）、`docs/specs/`（活跃规格）、`docs/research/`（调研）、`docs/legacy/`（历史归档）、`docs/session-state.md`、`docs/INDEX.md`。旧路径（`docs/ai/rules/`、`docs/superpowers/specs/`、`docs/superpowers/plans/` 等）仅作为历史指称出现。

## 摘要

1. 共记录 22 项问题：P0 × 4、P1 × 8、P2 × 6、P3 × 4；其中本轮已修 4 项，4 项由并行 agent 修复中，其余待修或待产品决策。
2. 最严重问题是历史泄露的真实供应商 API key（豆包 / Kimi / DeepSeek / GLM / 通义，seed profile 硬编码 + Python 测试 8 处硬编码凭据）。涉事文件已随 commit 621168f 整体删除，但 key 仍存活于 git 历史与 GitHub 远程——吊销 / 轮换是唯一必须由用户线下完成的 P0 动作。
3. 工程基建存在系统性真空：`.github/workflows` 不存在（无 CI），无任何自动化测试；历史验证方式是在生产服务器 Docker 容器内手工跑（反模式）。当前门禁只剩静态检查：check:surfaces + lint + typecheck + build。
4. 死代码清理本轮完成：`archive/` 与 `mcp-servers/` 整体删除（621168f）；6 个不可运行的 Vitest 残留测试清空（d3a6f0e，-714 行）。
5. 分支债清理：feature/a2ui-config-page（11 提交）已合回 main（1ba955c），其余 4 个过期分支已并入或为空。
6. 三项 P1 由并行 agent 本轮修复中：env 收口（7 处裸 `process.env`）、配置漂移（tsconfig / eslint / .gitignore / 脚本 / vercel.json）、死依赖 postgres（0 引用 + 指向不存在的 schema.pg.ts）。
7. 架构核心债是 24 处 `as unknown as` 双重断言，集中于 GameModule 插件边界与 memory 序列化；根因是泛型契约与具体游戏类型不协变，重构方向为判别联合或 per-game 泛型注册，需单独立项。
8. 已知用户可见 bug：大厅空态文案写「9 人狼人杀」，而狼人杀引擎固定 6 人（page.tsx:73 vs roles.ts:41-42）。
9. 部署债集中在生产环境：裸 IP HTTP 无 TLS（域名审核未过）、backup.sh 曾 Permission denied、`.mimosa/` 等工具目录未 ignore；Vercel fallback 从未实际部署验证。
10. 文档债（23 份 plan 中 7 份 checkbox 失真、PRD 缺失、AGENTS.md 旧路径）由本轮文档重构一并处理，本文档即新文档体系的组成部分。

### 状态统计

| 状态 | 编号 | 数量 |
|------|------|------|
| 本轮已修✅ | 01、05、17、18 | 4 |
| 修复中（并行 agent） | 06、07、08、16 | 4 |
| 待修 | 03、04、09、10、11、12、13、14、15、20、22 | 11 |
| 待产品决策（含部分待决策） | 02、15、19、21 | 3（与待修有交集） |

| 等级 | 数量 | 编号 |
|------|------|------|
| P0 | 4 | 01✅、02、03、04 |
| P1 | 8 | 05✅、18✅、06、07、08、09、10、11 |
| P2 | 6 | 17✅、12、13、14、15、16 |
| P3 | 4 | 19、20、21、22 |

## 问题总表

| 编号 | 等级 | 类别 | 问题 | 证据位置 | 修复方案 | 状态 |
|------|------|------|------|----------|----------|------|
| 01 | P0 | 安全-凭据 | 真实形态 API key 泄露：豆包/Kimi/DeepSeek/GLM/通义 seed profile 硬编码；另 8 处硬编码凭据 | 原 archive/old/src/store/profile-store.ts；原 mcp-servers/vision-router/tests/test_config.py | 整体删除 archive/ 与 mcp-servers/ | 本轮已修✅（621168f） |
| 02 | P0 | 安全-凭据 | 涉事 key 仍在 git 历史与 GitHub 远程，删除文件不清除历史 | 621168f 之前的历史提交 | 各供应商平台吊销/轮换（用户动作）；可选 git filter-repo + force push 清史 | 待修（轮换）/ 待产品决策（清史） |
| 03 | P0 | 工程基建 | 无 CI：.github/workflows 不存在，历史上靠生产服务器 Docker 内跑验证（反模式） | 仓库根 .github/ | 等 docs/research/ CI 调研报告落地后建 GitHub Actions，lint/typecheck/build 先行 | 待修 |
| 04 | P0 | 质量门禁 | 测试体系真空：门禁 = check:surfaces+lint+typecheck+build，无任何自动化测试 | package.json scripts；src/ 无可运行测试 | 按调研报告分阶段重建：引擎/解析器单测 → API → E2E | 待修 |
| 05 | P1 | 测试-死代码 | 死测试体系：Vitest 2026-06-05 整体删除后残留 6 个不可运行 *.test.ts（vitest 不在依赖、被 tsconfig 排除出类型检查）；npm test 为 echo 占位 | 原 src/ 下 6 个 *.test.ts | 清空残留测试与占位脚本（-714 行） | 本轮已修✅（d3a6f0e） |
| 06 | P1 | 环境变量 | env 收口未完成：src/platform/env.ts 之外 7 处裸 process.env；M4_MOCK_LLM 与 LOG_LEVEL 不在 zod schema | llm-runtime.ts:35、auth/match-token.ts:4、redis/adapter.ts:42、redis/upstash-adapter.ts:12-13、telemetry/logger.ts:8 | 全部纳入 zod schema，统一从 env.ts 读取 | 修复中（并行 agent） |
| 07 | P1 | 配置漂移 | tsconfig 死 include/exclude、eslint 死 ignore、.gitignore old/ 失配、脚本 old/ 假设、vercel.json 与 next.config 产物目录不匹配 | tsconfig.json、eslint 配置、.gitignore、scripts/git-step-commit.mjs:38、vercel.json vs next.config | 逐一清理失配项，统一指向 .next-build | 修复中（并行 agent） |
| 08 | P1 | 死依赖 | postgres 依赖 0 引用；drizzle pg 分支指向不存在的 schema.pg.ts；根 docker-compose 起无人用的 postgres:16 | package.json、drizzle 配置、docker-compose | 卸载依赖、删 pg 分支、清理 compose 服务 | 修复中（并行 agent） |
| 09 | P1 | 用户可见 bug | 大厅空态文案「9 人狼人杀」，引擎固定 6 人 | src/app/page.tsx:73 vs games/werewolf/engine/roles.ts:41-42 | 文案改为 6 人（或改为从引擎常量注入） | 待修 |
| 10 | P1 | 部署-env | .env.vercel.example 含 2 个零引用变量；Vercel fallback 从未实际部署验证（无账号），流程未跑通 | .env.vercel.example（SYSTEM_MODERATOR_BASE_URL / SYSTEM_MODERATOR_MODEL）；docs/legacy/ Vercel 文档 | 删除零引用变量（或随 Moderator 种子功能一并实现，见 15）；fallback 文档标注「未验证」直至真实跑通 | 待修 |
| 11 | P1 | 部署-运维 | 生产裸 IP HTTP 无 TLS（域名审核未过，Caddyfile 域名块注释）；backup.sh 曾 Permission denied；.mimosa/ 等工具目录未 ignore | ops/deploy/Caddyfile、backup.sh、docs/session-state.md、.gitignore | 域名过审后启用 TLS 块；修脚本权限；补 ignore 规则 | 待修 |
| 12 | P2 | 架构 | 24 处 as unknown as 双重断言（GameModule 边界 + memory 序列化） | poker-plugin.ts:13,14,20、werewolf-plugin.ts:11,12、action-validator.ts:75,123、game-master.ts:351、matches.ts:74、events.ts:53、poker-engine.ts:110 等 | 契约层改判别联合（discriminated union）或 per-game 泛型注册；单独立项 spec | 待修 |
| 13 | P2 | 代码规模 | 巨型文件：poker-engine.ts 638 行、match-view-store.ts 609 行、game-master.ts 475 行 | 对应文件 | 按职责拆分（可与 12 重构合并执行） | 待修 |
| 14 | P2 | 类型 | ChipChart.tsx:87-88 全仓库唯一显式 any（recharts formatter） | ChipChart.tsx | 换成具体 formatter 签名 | 待修 |
| 15 | P2 | 功能挂账 | Moderator 种子缺失、keyring UX、semantic 记忆脚本、长局 checkpoint、thinking 可见性、SSE 错误通知、A2A 两个端点未实现 | 见下文第十五节逐项；原始记录见 docs/session-state.md | 逐项排期；两项需产品决策 | 待修 / 待产品决策 |
| 16 | P2 | 文档 | 23 份 plan 中 7 份 checkbox 全未勾但工作实际完成（记录失真）；PRD 缺失；AGENTS.md 引用旧路径 | docs/legacy/（原 plans）、docs/prd/、AGENTS.md | 本轮文档重构处理：plan 归档并补记实际状态、重建 PRD、更新 AGENTS.md | 修复中（本轮文档重构） |
| 17 | P2 | git | 本地 5 个过期分支，其中 feature/a2ui-config-page 含 11 个未合提交 | git branch 列表 | 1ba955c 合并回 main；其余 4 个已并入或空，可删 | 本轮已修✅（1ba955c） |
| 18 | P1 | 死代码 | 孤儿项目 mcp-servers/vision-router（Python MCP server，全仓库零引用） | 原 mcp-servers/ | 随 621168f 一并删除 | 本轮已修✅（621168f） |
| 19 | P3 | LLM 配置 | 共享 maxOutputTokens=2048 对冗长推理模型可能截断；上调影响扑克成本 | LLM 共享配置 | 改 per-game / per-profile 配置 | 待产品决策 |
| 20 | P3 | 类型 | WerewolfRoster 一处 as string（cosmetic） | WerewolfRoster | 收紧类型 | 待修 |
| 21 | P3 | 功能候选 | 旧版功能未恢复：跨对局历史、胜率/热力图（equity.ts 已算未用）、表情系统、第三游戏 | docs/legacy/ 旧前端归档；equity.ts | 列入产品 backlog 排期 | 待产品决策 |
| 22 | P3 | 运维 | 生产服务器旧 poker-arena 容器占用 :3000 | 生产服务器 docker ps | 清理/下线旧容器，避免端口冲突 | 待修 |

## 一、本轮已修复（4 项）

### 01 泄露凭据删除（P0 安全）

- 问题：`archive/old/src/store/profile-store.ts` 的 seed profile 硬编码了真实形态的供应商 API key（豆包 / Kimi / DeepSeek / GLM / 通义）；`mcp-servers/vision-router/tests/test_config.py` 另有 8 处硬编码凭据。
- 处置：commit 621168f 删除整个 `archive/` 与 `mcp-servers/` 目录，工作区已无泄露源。
- 遗留：见 02，key 轮换未完成前本项只能视为「半关闭」。

### 05 死测试体系清空（P1）

- 问题：旧 Vitest 体系于 2026-06-05 被整体删除，但残留 6 个 `*.test.ts`：vitest 不在 dependencies 中无法运行，且被 tsconfig 排除出类型检查（连编译错误都不暴露）；`npm test` 为 echo 占位，制造「有测试」的假象。
- 处置：commit d3a6f0e 清空全部残留（-714 行），`npm test` 不再伪造通过。
- 关联：测试的真空中重建见 04。

### 17 git 分支债（P2）

- 本地 5 个过期分支中，feature/a2ui-config-page 携带 11 个提交，已于 commit 1ba955c 合并回 main。
- 其余 4 个分支经核查均已并入 main 或无独有提交，可安全删除（保留删除动作为后续清理项，不阻塞）。

### 18 孤儿项目删除（P1 死代码）

- `mcp-servers/vision-router` 是一个 Python MCP server，全仓库零引用（无 import、无脚本、无文档指向），维护成本与误读风险纯负。已随 621168f 一并删除。

## 二、P0 待修（3 项）

### 02 API key 吊销 / 轮换 + 可选清史

- 事实：`git rm` 只移除当前快照。621168f 之前的历史提交仍完整保留两个涉事文件的 blob；GitHub 远程仓库同样持有。任何获得仓库读权限的主体都可恢复这些 key。
- 修复（按优先级）：
  1. 【用户动作，不可代理】在各供应商平台（豆包 / Kimi / DeepSeek / GLM / 通义）吊销全部涉事 key 并签发新 key；新 key 只走本地 env，绝不入库。
  2. 【待产品决策】git 历史清除：`git filter-repo` 删除两个路径的全历史 + force push。破坏性操作（所有 clone 失效、开源协作者需重新克隆）。在「仓库私有 + key 已轮换」前提下，清史为可选项而非必选项。
- 关闭条件：全部 key 轮换完成并在本表标记。

### 03 CI 缺失

- 证据：仓库根无 `.github/workflows` 目录。历史上质量验证依赖「生产服务器 Docker 容器内手工执行」——生产环境兼任 CI：结果不可追溯、无法阻止坏提交合入、且与生产部署耦合。
- 修复：等待 `docs/research/` 的 CI 调研报告落地后建 GitHub Actions：
  - 第一阶段（无测试也可上线）：lint + typecheck + check:surfaces + build，push/PR 触发。
  - 第二阶段：接入测试 job（依赖 04 的测试重建）。

### 04 测试体系真空

- 现状：门禁 = check:surfaces + lint + typecheck + build，全部为静态检查，无任何运行时验证。回归风险完全靠人工目测。
- 修复：按 `docs/research/` 调研报告分阶段重建：
  1. 引擎 / 解析器纯函数单测（games/*/engine、A2A 消息解析）——收益最高、依赖最少；
  2. API route 集成测试（match 生命周期、auth）；
  3. E2E（Playwright 覆盖建局 → 观赛 → 结算主链路）。

## 三、P1 待修（6 项）

### 06 env 收口（修复中）

- `src/platform/env.ts`（zod schema）是唯一合法 env 入口，但仍有 7 处裸 `process.env`：
  - llm-runtime.ts:35（`M4_MOCK_LLM`）
  - auth/match-token.ts:4
  - redis/adapter.ts:42
  - redis/upstash-adapter.ts:12-13
  - telemetry/logger.ts:8（`LOG_LEVEL`）
- 其中 `M4_MOCK_LLM` 与 `LOG_LEVEL` 完全不在 schema 中（拼写错误不会被发现、无默认值约束）。
- 修复：全部纳入 schema，调用方统一改读 env.ts 导出值。状态：并行 agent 本轮修复中，完成后回归 typecheck + build 验证。

### 07 配置漂移（修复中）

五处失配，全部指向已删除的 old/ 体系或不一致的构建目录：

1. tsconfig.json：死 include（`.next/types`、`.next-build/types`）与死 exclude（`old`、`dist`）。
2. eslint：死 ignore（old / dist / build / .pytest_cache）。
3. .gitignore：old/ 系列模式失配（目录已删，规则成噪音）。
4. scripts/git-step-commit.mjs:38：假设 old/ 路径存在。
5. vercel.json `outputDirectory=.next` 与 next.config `distDir=.next-build` 不匹配——若真走 Vercel 部署会找不到产物。

修复：逐一清理并统一目录约定。状态：并行 agent 本轮修复中。

### 08 死依赖 postgres（修复中）

- `postgres` 包 0 代码引用；drizzle 的 pg 分支指向不存在的 `schema.pg.ts`；根 docker-compose 仍起 postgres:16 容器（本地资源浪费 + 误导新协作者以为项目用 PG；实际为 Redis + 现有方案的其它存储路径）。
- 修复：卸载依赖、删除 pg 配置分支、清理 compose 服务。状态：并行 agent 本轮修复中。

### 09 狼人杀人数文案 bug

- src/app/page.tsx:73 大厅空态写「9 人狼人杀」；games/werewolf/engine/roles.ts:41-42 引擎固定 6 人。用户可见的事实性错误。
- 修复：文案改为 6 人；更稳的做法是文案从引擎导出的常量注入，杜绝再次漂移。

### 10 Vercel fallback 未验证 + 零引用 env 变量

- `.env.vercel.example` 中 `SYSTEM_MODERATOR_BASE_URL` / `SYSTEM_MODERATOR_MODEL` 在代码中零引用（属 Moderator 种子功能未落地的残留，见 15 第一项）。
- Vercel fallback 方案（文档归档于 docs/legacy/）从未实际部署验证（无账号），流程未跑通；叠加 07 的 outputDirectory 失配，当前照文档操作大概率失败。
- 修复：删除零引用变量（或等 Moderator 种子功能落地时一并实现）；fallback 文档显式标注「未验证」，待有账号后真实跑通一次再摘标。

### 11 生产部署债

- 无 TLS：生产以裸 IP HTTP 提供服务（域名审核未过，ops/deploy/Caddyfile 的域名块处于注释状态）。域名过审后取消注释、签发证书并重载 Caddy。
- backup.sh 曾 Permission denied（docs/session-state.md 有记录）：核查 cron 运行用户与脚本 / 目标目录权限，补一次性验证。
- `.mimosa/` 等本地工具目录未 ignore，存在误提交风险（本轮审计即在脏工作区风险下进行）。

## 四、P2 待修（5 项）

### 12 `as unknown as` 双重断言 × 24

- 分布：集中于 poker-plugin / werewolf-plugin 的 GameModule 边界与 memory 序列化。代表位置：src/games/poker/poker-plugin.ts:13,14,20；src/games/werewolf/werewolf-plugin.ts:11,12；action-validator.ts:75,123；game-master.ts:351；platform/db/queries/matches.ts:74；events.ts:53；poker-engine.ts:110。
- 根因：泛型 `GameModule` 契约与具体游戏类型不协变（TS 泛型不变性 + 各游戏事件 / 状态 payload 差异），插件层被迫双重断言绕过类型系统，类型安全在边界处失效。
- 重构方向：契约层改判别联合（按 gameKind 收窄事件与状态类型），或 per-game 泛型注册表。影响面大，建议先在 `docs/specs/` 立专项 spec 再动代码。

### 13 巨型文件

- poker-engine.ts 638 行、match-view-store.ts 609 行、game-master.ts 475 行。
- 拆分建议按职责切（引擎纯逻辑 / 状态机流转 / IO 与编排），可与 12 的契约重构合并执行，避免二次返工。

### 14 唯一显式 any

- ChipChart.tsx:87-88（recharts formatter 参数）。全仓库仅此一处显式 any，修掉后可在 eslint 层面禁绝 `any`（含 `as unknown as` 的渐进治理参照 12）。

### 15 功能挂账（源自 docs/session-state.md 历史记录）

1. Moderator 默认种子缺失：创建狼人杀局前必须手工创建主持人 profile——应内置默认种子（与 10 的零引用 env 变量同源）。
2. keyring 状态 UX：key 缺失 / 过期时无引导重传，用户只能看裸错误。
3. semantic 记忆重建脚本缺失。
4. 长局性能 checkpoint 未做：完整 events 重放影响首屏加载。
5. agent / thinking 事件可见性策略未定。【待产品决策】
6. SSE 轻量错误通知未做：现靠 ErrorBadge 轮询感知错误。
7. A2A spec 承诺的 `message:send` 非流式端点与 `tasks/:id` 查询端点未实现（spec 与实现漂移）。

### 16 文档债（修复中，本轮文档重构处理）

- 23 份 plan（现归档 `docs/legacy/`）中 7 份 checkbox 全未勾但工作实际完成——进度记录失真，AI 协作时按 checkbox 判断会得出错误结论。
- PRD 缺失：本轮在 `docs/prd/` 重建。
- AGENTS.md 引用旧路径（docs/ai/rules/ 等）：本轮随目录重构同步更新为新路径。
- 后续维护以 `docs/INDEX.md` 为路由入口，`docs/session-state.md` 记录长任务状态。

## 五、P3（4 项）

- 19 共享 `maxOutputTokens=2048`：对冗长推理模型可能截断思考 / 发言；直接上调会推高扑克局成本。需 per-game（或 per-profile）配置化。【待产品决策】
- 20 WerewolfRoster 一处 `as string`（cosmetic 级类型松动），随手修。
- 21 旧版功能未恢复候选（参考 `docs/legacy/` 旧前端归档）：跨对局历史浏览页、胜率 / 热力图（equity.ts 已计算但 UI 未消费）、表情系统、第三游戏。【待产品决策，列入 backlog】
- 22 生产服务器旧 poker-arena 容器占用 :3000，与新部署存在端口冲突风险，择机清理。

## 六、修复路线图建议

1. 第一批（立即，本周内）：02 key 轮换（用户线下动作，最高优先）→ 验收 06/07/08 三项修复中的回归（typecheck + build）→ 09 文案修正 → 11 中 .gitignore 补 `.mimosa/` 与 backup.sh 权限核查。
2. 第二批（依赖调研）：03 CI 落地（等 `docs/research/` 报告）→ 04 测试重建第一阶段（引擎 / 解析器单测）。
3. 第三批（架构专项）：12 断言重构先在 `docs/specs/` 立 spec → 13 巨型文件拆分随 12 一并执行 → 14 顺手关闭并上 eslint 禁 any 规则。
4. 第四批（产品参与）：15 挂账逐项排期，其中「thinking 可见性」「Moderator 种子形态」需产品决策；19、21 同批决策。
5. 持续项：P3 其余项择机处理；文档类维护移交 `docs/INDEX.md` + `docs/session-state.md` 常态机制；git 历史清除（02 可选部分）待产品决策后单独执行。

## 七、复核与验证方式（附录）

- 状态变更流程：任一编号修复完成后，更新问题总表「状态」列与对应小节，并在 `docs/session-state.md` 记录一笔；涉及架构决策的（12、15-5、19、21）须先在 `docs/specs/` 或 `docs/prd/` 落规格再动代码。
- 修复验收基线（本轮审计时点的门禁命令）：
  - `npm run lint`
  - `npm run typecheck`（等价于 tsc --noEmit 路径，以 package.json scripts 为准）
  - `npm run check:surfaces`（前后端边界红线检查）
  - `npm run build`
- 06/07/08（修复中三项）验收时须额外确认：全仓库 grep `process.env` 仅剩 env.ts 一处合法出口；tsconfig / eslint / .gitignore / vercel.json 无 old/、dist、.next 失配残留；`postgres` 不在依赖树、根 compose 无 postgres 服务。
- 02 的关闭判定：各供应商控制台确认涉事 key 已吊销且新 key 未入库（`git log -p` 全历史 grep 关键词 + 工作区扫描）；若执行清史，需确认 force push 后所有本地 clone 重建。
- 本审计未执行任何 npm build/test 与 git 状态变更操作，基线证据来自只读检查（文件内容、git log、分支列表）。

---

本审计由 AI 协作流程生成，事实均于 2026-09-07 在 main 分支（a2ui 合并后）核验。后续修复请以本表编号跟踪状态，状态变更时同步更新本文与 `docs/session-state.md`。
