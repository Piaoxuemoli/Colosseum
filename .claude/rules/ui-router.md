# UI Rule Router

For UI, component, page, animation, or styling work, read:

- `docs/ai/rules/ui-style.md`

If the UI work consumes APIs, SSE, Route Handlers, or server state, also read:

- `docs/ai/rules/frontend-backend.md`
